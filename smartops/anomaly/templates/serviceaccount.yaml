{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "smartops-anomaly.serviceAccountName" . }}
  labels:
    {{- include "smartops-anomaly.labels" . | nindent 4 }}
{{- end }}
