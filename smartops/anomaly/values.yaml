# Default values for smartops-anomaly
image:
  repository: us-docker.pkg.dev/icbt-465109/smartops-repo/smartops-anomaly
  tag: "0f217bf"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8000

resources:
  requests:
<<<<<<< HEAD
    memory: "8Mi"
    cpu: "5m"
  limits:
    memory: "16Mi"
    cpu: "10m"

replicaCount: 1

# Autoscaling configuration
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 1

# Model configuration
model:
  enabled: false
  mountPath: "/app/model"
  pvcName: "anomaly-model-pvc"

# Pod annotations
podAnnotations: {}

# Image pull secrets
imagePullSecrets:
  - name: gcp-registry-secret

# Service account configuration
serviceAccount:
  create: true
  name: ""

# Pod security context
podSecurityContext: {}

# Container security context
securityContext: {}

# Environment variables
env: []

# Node selector
nodeSelector: {}

# Affinity
affinity: {}

# Tolerations
tolerations: []
=======
    memory: "256Mi"
    cpu: "200m"
  limits:
    memory: "512Mi"
    cpu: "500m"

model:
  enabled: true
  mountPath: /app/model
  pvcName: smartops-anomaly-model

replicaCount: 1
>>>>>>> d5968d9ecc2659dc2b13f28fedbfd906d03a371e
