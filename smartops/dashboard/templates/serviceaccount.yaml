{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "smartops-dashboard.serviceAccountName" . }}
  labels:
    {{- include "smartops-dashboard.labels" . | nindent 4 }}
{{- end }}
