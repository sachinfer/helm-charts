# Default values for smartops-monitor
image:
  repository: us-docker.pkg.dev/icbt-465109/smartops-repo/smartops-monitor
  tag: "0f217bf"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8000

resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "256Mi"
    cpu: "200m"

replicaCount: 1
